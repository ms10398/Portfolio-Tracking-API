// const Trade = require('../models/trade.model');
// const Security = require('../models/security.model');

// module.exports = {

//   // getPortfolio: async(req, res, next) => {
//   //   const securities = await Security.find().select('ticker');
//   // },

// };
